import React, { Component } from 'react';
import Router, { Link, RouteHandler } from 'react-router';

// components

export default class mainEntities extends Component {
  constructor(props) {
    super(props);
  }

  render() {

    return (
      <main>list of all Entities</main>
    );
  }
}
